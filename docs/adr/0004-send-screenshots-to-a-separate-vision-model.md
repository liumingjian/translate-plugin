---
status: accepted
---

# 截图直接交给独立配置的视觉模型

截图翻译把用户选定的网页画面直接发送给截图模型，而不是在扩展内运行 OCR 后复用文本翻译链路。视觉模型更能处理复杂版面、混合内容和缺少文本层的画面，也避免引入体积和维护成本较高的本地 OCR；代价是图片会发送至用户配置的翻译服务，且截图模型必须支持图像输入。截图模型与文本模型共用服务地址和访问凭据，但模型名称分别配置；图片只在内存中处理，不落盘，也不进入翻译缓存。
